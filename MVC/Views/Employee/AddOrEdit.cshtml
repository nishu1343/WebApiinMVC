@model MVC.Models.mvcEmployeeModel
@{
    ViewBag.Title = "AddOrEdit";
}

<h2>AddOrEdit</h2>

<div class="form-body">
    @using (Html.BeginForm())
    {
        @* Hidden field for employeeid*@
        @Html.HiddenFor(model => model.EmployeeId)

        <div class="form-group">
            @Html.LabelFor(model => model.Name)
            @Html.EditorFor(model => model.Name)
            @*add validation control to this property*@
            @Html.ValidationMessageFor(model => model.Name)
        </div>
        <div class="form-group">
            @Html.LabelFor(model => model.Position)
            @Html.EditorFor(model => model.Position)
            @*add validation control to this property*@
            @Html.ValidationMessageFor(model => model.Position)
        </div>
        <div class="form-group">
            @Html.LabelFor(model => model.Age)
            @Html.EditorFor(model => model.Age)
            @*add validation control to this property*@
            @Html.ValidationMessageFor(model => model.Age)
        </div>
        <div class="form-group">
            @Html.LabelFor(model => model.Salary)
            @Html.EditorFor(model => model.Salary)
            @*add validation control to this property*@
            @Html.ValidationMessageFor(model => model.Salary)
        </div>
        <div class="form-group">
            <input type="submit" value="Submit" class="btn button" />
            <input type="reset" value="Reset" class="btn button" />
        </div>
    }
</div>
@*Add jquery validation script files inside its addorEdit view.Go to script section inside Layout.cshtml in shared folder.get scripts from there
    inside we have to pass the bundle for jquery validation. In AppStart folder< BundleConfig.cs. Copy(~/bundles/jqueryval) the bundle for jquery validation files
    and render that bundle inside*@
@section scripts{
    @Scripts.Render("~/bundles/jqueryval");
    }

